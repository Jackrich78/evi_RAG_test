{
  "permissions": {
    "allow": [
      "Bash(docker exec evi_rag_postgres psql -U postgres -d evi_rag -c \"\\dt\")",
      "Bash(awk '{print $9, $5}')",
      "Bash(source venv/bin/activate)",
      "Bash(python3 -c \"from agent.models import TieredGuideline, GuidelineSearchResult, EVIProduct, ProductRecommendation, ProductSearchResult, ResearchAgentResponse, SpecialistAgentResponse; print(''✅ All EVI models import successfully'')\")",
      "Bash(python3 -c \"from config.notion_config import NotionConfig; print(''✅ NotionConfig imports successfully'')\")",
      "Bash(python3 -c \"import agent.models; import inspect; models = [m for m in dir(agent.models) if inspect.isclass(getattr(agent.models, m))]; evi_models = [m for m in models if m in [''TieredGuideline'', ''GuidelineSearchResult'', ''EVIProduct'', ''ProductRecommendation'', ''ProductSearchResult'', ''ResearchAgentResponse'', ''SpecialistAgentResponse'']]; print(f''✅ Found {len(evi_models)} EVI-specific models: {evi_models}'')\")",
      "Bash(python3 -m pytest tests/agent/test_evi_models.py -v)",
      "Bash(docker-compose -f docker-compose.yml config --quiet)",
      "Bash(echo \"Exit code: $?\")"
    ],
    "deny": [],
    "ask": []
  }
}
